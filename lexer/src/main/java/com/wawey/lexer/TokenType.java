package com.wawey.lexer;

/**
 * @author Tomas Perez Molina
 */
enum TokenType {
    STRING_LITERAL,
    NUMBER_LITERAL,
    STRING_TYPE,
    NUMBER_TYPE,
    IDENTIFIER,
    PLUS,
    MINUS,
    ASTERISK,
    FORWARD_SLASH,
    LEFT_PAREN,
    RIGHT_PAREN,
    EQUALS,
    COLON,
    SEMICOLON,
    LET,
    PRINT,
}
